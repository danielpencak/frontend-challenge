import PropTypes from 'prop-types';

const PropertyLeasesTable = (selectedPropertyId) => {
  return 'Table';
};

PropertyLeasesTable.propTypes = {
  selectedPropertyId: PropTypes.number,
};

export default PropertyLeasesTable;
